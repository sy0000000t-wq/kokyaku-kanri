/**
 * 画面に出すバージョン。
 * どの版が端末に入っているか（更新が届いたか）を確かめるために使う。
 * リリースのたびに上げ、同じ名前の git タグを打つ。
 * 小数点以下を 1 ずつ足していき（V1.0 → V1.1 → V1.2…）、
 * 大きな作り替えのときだけ整数を上げる（V1.x → V2.0）。
 */
export const APP_VERSION = "V1.3";
